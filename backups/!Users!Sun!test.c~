#include <stdio.h>
#define MAX(a, b) a>b? a : b
#define str "Hello world"

void ss(char *s)
{
        printf ("%s\n", s);;
}

int main(int argc, char *argv[])
{
	int a = 2;
	int b = 2;
	int c = 1;
        
	int d = MAX(++a, c);
        
        ss(str);
        return 0;
}

