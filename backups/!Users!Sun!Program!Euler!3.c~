#include "stdio.h"
#include <math.h>
int prime(int a){
  int n;
  for (n = 0; n < a; n++)
    {
      if(a % n ==0)
        return 0;
    }
  return 1;
}

int main(int argc, char *argv[])
{
  int num = 600851475143;
  int n = sqrt(num);
  int i;
  for( i=2; i<n; i++){
    while ((num%i == 0) && (prime(i) == 1)){
      num /= i;
    }
  }
  printf ("%d\n", num);
  return 0;
}
