#include "stdio.h"
int main(int argc, char *argv[])
{
  int a;
  int sum1 = 0;
  int sum2 = 0;
  for (a = 0; a < 100; a++)
    {
      sum1 += a*a;
    }

  for (a = 0; a < 100; a ++)
    {
      sum2 += a;
    }

  printf ("%d\n", sum1 - sum2*sum2);
  return 0;
}
